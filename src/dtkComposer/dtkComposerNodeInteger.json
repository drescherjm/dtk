{
          "title" : "Integer",
           "kind" : "atomic",
           "type" : "integer",
           "tags" : ["integer", "long int", "int", "short int", "atomic"],
    "description" : "<p>Integer node handles 64-bit int type.</p>
                     <p>When used as a source node (ie without any edge at the input), one can set the integer value through the node editor.</p>
                     <p>Otherwise, one can feed its input either with a boolean, integer, double or string value. 
                        When necessary, the node peform a conversion and returns the corresponding integer value.</p>",
         "inputs" : ["value"],
        "outputs" : ["value"]
}
